import {Component, OnInit} from '@angular/core';

@Component({
  selector: 'base-layout',
  template: `base-layout.component.html`
})

export class BaseLayout implements OnInit {
  constructor() {
  }

  ngOnInit() {
  }
}
